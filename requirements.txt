# This file is kept for compatibility but uv will use pyproject.toml dependencies
# For uv usage: uv sync or uv sync --group dev

# Core dependencies
music21>=9.1.0
librosa>=0.10.0
soundfile>=0.12.0
basic-pitch>=0.2.5